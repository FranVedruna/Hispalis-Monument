<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color de fondo -->
    <solid android:color="#C78570" />

    <!-- Bordes redondeados -->
    <corners android:radius="12dp" />

    <!-- Borde de 2dp de grosor, color hispanis_dark -->
    <stroke
        android:width="5dp"
        android:color="@color/hispanis_accent" />

</shape>